### React Native

``` shell
com.android.builder.testing.api.DeviceException:
 com.android.ddmlib.InstallException:
  Failed to finalize session : -26: Package {PACKAGE_NAME} new target SDK 22 doesn't support runtime permissions but the old target SDK 23 does.
```

